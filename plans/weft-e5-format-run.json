{
  "name": "weft-e5-format-run",
  "claims": [
    {
      "id": "WFT-FORMAT",
      "description": "src/format.mjs: format(src) idempotent and parse-stable per DESIGN.md section 10 — 60-char one-line rule, 2-space continuation indent, one blank line between top-level forms, trailing newline, strings re-escaped, comments dropped"
    }
  ],
  "tasks": [
    {
      "key": "formatter",
      "description": "Create src/format.mjs with format(src) per DESIGN.md section 10. Tests in tests/format.test.mjs pin idempotence and parse-stability on inputs exercising BOTH layout branches, plus string re-escaping and the blank-line/trailing-newline rules. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-FORMAT mutates=src/format.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-FORMAT"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/format.mjs",
        "tests/format.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "run-api",
      "description": "Extend src/index.mjs per DESIGN.md section 9: add run(src) returning { value, output } (output joins __out) and re-export format. Tests in tests/run-api.test.mjs cover multi-print ordering, print returning nil, and run on a list-valued program. (Glue — no mutation claim; verified behaviorally by the suite.)",
      "claims": [],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/index.mjs",
        "tests/run-api.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
