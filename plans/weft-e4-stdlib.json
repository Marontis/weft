{
  "name": "weft-e4-stdlib",
  "claims": [
    {
      "id": "WFT-RENDER",
      "description": "src/render.mjs: render(value) canonical per DESIGN.md section 8 — integral floats without a decimal point, strings as-is, nil, booleans, nested lists parenthesized, functions <fn>"
    },
    {
      "id": "WFT-CORE",
      "description": "src/builtins-core.mjs: + - * / (variadic incl. unary, division-by-zero error), = deep structural, < > <= >= number-only chained, not, str, print per DESIGN.md section 8"
    },
    {
      "id": "WFT-LISTS",
      "description": "src/builtins-lists.mjs: list/head/tail/cons/len/nth/concat/reverse (errors, non-mutating) + map/filter/reduce (left fold, required init) per DESIGN.md section 8"
    },
    {
      "id": "WFT-STRINGS",
      "description": "src/builtins-strings.mjs: str-len/str-slice(clamped)/str-split/str-join/str-upper/str-lower/str-contains? with 'expected string' error per DESIGN.md section 8"
    },
    {
      "id": "WFT-MATH",
      "description": "src/builtins-math.mjs: mod (JS % sign)/floor/ceil/abs/min/max per DESIGN.md section 8"
    }
  ],
  "tasks": [
    {
      "key": "render",
      "description": "Create src/render.mjs with render(value) per DESIGN.md section 8. Tests in tests/render.test.mjs pin 42 vs 42.5, negatives/fractions, strings verbatim, nil, booleans, nested lists, <fn>. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-RENDER mutates=src/render.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-RENDER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/render.mjs",
        "tests/render.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "builtins-core",
      "description": "Create src/builtins-core.mjs (+ - * / = < > <= >= not str print; str/print delegate to src/render.mjs), register them in src/env.mjs newGlobalEnv, and create src/index.mjs exporting tokenize, parse, WeftError, newGlobalEnv, evaluate(src, env?) per section 9. Tests in tests/builtins-core.test.mjs run THROUGH evaluate(): unary minus/reciprocal, division-by-zero error, chained comparisons, deep structural =, print output. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-CORE mutates=src/builtins-core.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-CORE"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/builtins-core.mjs",
        "src/env.mjs",
        "src/index.mjs",
        "tests/builtins-core.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "stdlib-lists",
      "description": "Create src/builtins-lists.mjs (list/head/tail/cons/len/nth/concat/reverse + map/filter/reduce per DESIGN.md section 8) and register in src/env.mjs newGlobalEnv. Tests in tests/stdlib-lists.test.mjs run THROUGH evaluate(), assert empty-list/out-of-range errors, pin non-mutation, and use real (fn ...) closures incl. reduce's required init. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-LISTS mutates=src/builtins-lists.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-LISTS"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/builtins-lists.mjs",
        "src/env.mjs",
        "tests/stdlib-lists.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "stdlib-strings",
      "description": "Create src/builtins-strings.mjs per DESIGN.md section 8 and register in src/env.mjs. Tests in tests/stdlib-strings.test.mjs run THROUGH evaluate(): slice clamping both ends, split/join round-trip, case ops, contains? true/false, 'expected string' error. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-STRINGS mutates=src/builtins-strings.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-STRINGS"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/builtins-strings.mjs",
        "src/env.mjs",
        "tests/stdlib-strings.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "stdlib-math",
      "description": "Create src/builtins-math.mjs per DESIGN.md section 8 and register in src/env.mjs. Tests in tests/stdlib-math.test.mjs run THROUGH evaluate() and pin mod's JS % sign on negatives, floor/ceil/abs on negatives, min/max with one and several args. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-MATH mutates=src/builtins-math.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-MATH"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/builtins-math.mjs",
        "src/env.mjs",
        "tests/stdlib-math.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
