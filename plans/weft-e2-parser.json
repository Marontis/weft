{
  "name": "weft-e2-parser",
  "claims": [
    {
      "id": "WFT-PARSER",
      "description": "src/parser.mjs: parse(src) per DESIGN.md section 3 — exact AST node shapes with positions, 'x -> (quote x) at the quote position, unclosed ( and unexpected ) errors at the right positions/prefixes"
    }
  ],
  "tasks": [
    {
      "key": "parser-core",
      "description": "Create src/parser.mjs with parse() per DESIGN.md section 3: exact node shapes {t,v/name/items,line,col}, arbitrary nesting, any number of top-level forms. Tests in tests/parser.test.mjs deep-equal whole ASTs incl. positions. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-PARSER mutates=src/parser.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-PARSER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/parser.mjs",
        "tests/parser.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "parser-quote",
      "description": "Extend src/parser.mjs with ' sugar per DESIGN.md section 3: 'x -> (quote x) positioned at the quote char, works nested. Tests in tests/parser-quote.test.mjs. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-PARSER mutates=src/parser.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-PARSER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/parser.mjs",
        "tests/parser-quote.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "parser-errors",
      "description": "Extend src/parser.mjs with the two error contracts of DESIGN.md section 3 (unclosed ( / unexpected ) at their positions, exact prefixes). Tests in tests/parser-errors.test.mjs assert prefix AND line:col. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-PARSER mutates=src/parser.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-PARSER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/parser.mjs",
        "tests/parser-errors.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
