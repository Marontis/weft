{
  "name": "weft-e1-lexer",
  "claims": [
    {
      "id": "WFT-LEXER",
      "description": "src/lexer.mjs: tokenize(src) per DESIGN.md sections 1-2 — correct token types/values, 1-based line:col at each token's first char, string escapes decoded, unterminated-string/unknown-escape errors at the right positions, ; comments skipped without disturbing positions"
    }
  ],
  "tasks": [
    {
      "key": "lexer-core",
      "description": "Create src/errors.mjs (WeftError per DESIGN.md section 4) and src/lexer.mjs with tokenize() per section 2 for numbers (incl. negatives/decimals), symbols, parens, the quote char and whitespace (strings/comments come in later tasks; a \" or ; may raise WeftError('not implemented', line, col) for now). Tests in tests/lexer.test.mjs pin token types, values, and exact 1-based line:col across multi-line input. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-LEXER mutates=src/lexer.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-LEXER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/errors.mjs",
        "src/lexer.mjs",
        "tests/lexer.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "lexer-strings",
      "description": "Extend src/lexer.mjs (read it first) with double-quoted strings per DESIGN.md sections 1-2: decode \\\" \\\\ \\n \\t, 'str' token at the opening quote; unterminated string and unknown escape raise WeftError at the exact positions with the spec's message prefixes. Tests in tests/lexer-strings.test.mjs. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-LEXER mutates=src/lexer.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-LEXER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/lexer.mjs",
        "tests/lexer-strings.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "lexer-comments",
      "description": "Extend src/lexer.mjs with ; comments per DESIGN.md section 1: from ; to end of line produces no tokens while line:col for later tokens stays exact. Tests in tests/lexer-comments.test.mjs pin a comment between tokens on one line and full-line comments. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-LEXER mutates=src/lexer.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-LEXER"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/lexer.mjs",
        "tests/lexer-comments.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
