{
  "name": "weft-e6-tco",
  "claims": [],
  "tasks": [
    {
      "key": "tco",
      "description": "Rework the apply/eval loop in src/evaluator.mjs into a trampoline so tail calls per DESIGN.md section 11 run in constant stack, WITHOUT changing observable non-tail semantics (whole suite stays green). tests/tco.test.mjs: self-recursive countdown to 100000 and even?/odd? mutual recursion to 100000. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-EVAL mutates=src/evaluator.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-EVAL"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/evaluator.mjs",
        "tests/tco.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "depth-cap",
      "description": "Add the non-tail depth cap per DESIGN.md section 11 to src/evaluator.mjs: 10000 nested NON-tail applies raise WeftError 'stack depth exceeded'; tail loops stay unaffected. tests/depth-cap.test.mjs pin both sides. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-EVAL mutates=src/evaluator.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-EVAL"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/evaluator.mjs",
        "tests/depth-cap.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
