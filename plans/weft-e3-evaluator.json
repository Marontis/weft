{
  "name": "weft-e3-evaluator",
  "claims": [
    {
      "id": "WFT-ENV",
      "description": "src/env.mjs: lexical environments per DESIGN.md section 6 — lookup walks the chain and raises 'unbound symbol: <name>' at the symbol's position, define binds in the current scope, newGlobalEnv seeds __out"
    },
    {
      "id": "WFT-EVAL",
      "description": "src/evaluator.mjs: evalForm per DESIGN.md sections 5+7 — quote yields plain data, if/and/or lazy and return the deciding value, def/let/fn/do with lexical closures and correct arity errors, application left-to-right rejecting non-functions, section-5 truthiness"
    }
  ],
  "tasks": [
    {
      "key": "env",
      "description": "Create src/env.mjs per DESIGN.md section 6 (lookup/define/newGlobalEnv; newGlobalEnv creates __out, builtins registered later). Tests in tests/env.test.mjs cover chained lookup, shadowing, the unbound-symbol error (message AND position), and current-scope-only define. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-ENV mutates=src/env.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-ENV"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/env.mjs",
        "tests/env.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "eval-core",
      "description": "Create src/evaluator.mjs (read src/env.mjs, src/parser.mjs) with evalForm for literals, symbol lookup (WFT-ENV error contract), and quote per DESIGN.md section 5 (symbol->name string, list->array of quoted items). Tests in tests/eval-core.test.mjs. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-EVAL mutates=src/evaluator.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-EVAL"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/evaluator.mjs",
        "tests/eval-core.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "eval-scope",
      "description": "Extend src/evaluator.mjs with def, let, do, fn and application per DESIGN.md section 7: closures capture the defining env, params bind in a child env, exact arity-error shape, sequential let, do returns last (empty do nil). Tests in tests/eval-scope.test.mjs pin closure capture and shadowing. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-EVAL mutates=src/evaluator.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-EVAL"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/evaluator.mjs",
        "tests/eval-scope.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    },
    {
      "key": "eval-branch",
      "description": "Extend src/evaluator.mjs with if, and, or per DESIGN.md section 7 and the section-5 truthiness rule. PROBE laziness with side effects (a def in the untaken branch must not run) and assert and/or return the deciding value. Tests in tests/eval-branch.test.mjs. Write MUTATION-ADEQUATE tests: assert exact values, exact token positions (line:col), exact error messages/positions, and boundary conditions — not just the happy path (a suite that still passes when an operator/constant/boundary is changed is not load-bearing and fails the gate). Put '// @attest WFT-EVAL mutates=src/evaluator.mjs' directly above the load-bearing test; never weaken or delete an existing assertion; the whole tests/ suite must stay green.",
      "claims": [
        "WFT-EVAL"
      ],
      "allowed_reads": [
        "docs/DESIGN.md",
        "src/**",
        "tests/**"
      ],
      "allowed_writes": [
        "src/evaluator.mjs",
        "tests/eval-branch.test.mjs"
      ],
      "verification_cmd": "node --test tests/*.test.mjs",
      "max_retries": 3
    }
  ]
}
